--- a/src/wrk.c
+++ b/src/wrk.c
@@ -1,5 +1,6 @@
 // Copyright (C) 2012 - Will Glozer.  All rights reserved.
 
+#include <time.h>
 #include "wrk.h"
 #include "script.h"
 #include "main.h"
@@ -550,5 +551,5 @@
 static uint64_t time_us() {
-    struct timeval t;
-    gettimeofday(&t, NULL);
-    return (t.tv_sec * 1000000) + t.tv_usec;
+    struct timespec t;
+    if (clock_gettime(CLOCK_MONOTONIC, &t) != 0) abort();
+    return (t.tv_sec * 1000000) + t.tv_nsec / 1000;
 }
--- a/src/ae.c
+++ b/src/ae.c
@@ -45,1 +45,7 @@
-#include "config.h"
+#include "config.h"
+
+static time_t monotonicSeconds(void) {
+    struct timespec t;
+    if (clock_gettime(CLOCK_MONOTONIC, &t) != 0) abort();
+    return t.tv_sec;
+}
@@ -66,1 +72,1 @@
-    eventLoop->lastTime = time(NULL);
+    eventLoop->lastTime = monotonicSeconds();
@@ -240,6 +246,6 @@
-    struct timeval tv;
+    struct timespec tv;
 
-    gettimeofday(&tv, NULL);
+    if (clock_gettime(CLOCK_MONOTONIC, &tv) != 0) abort();
     *seconds = tv.tv_sec;
-    *milliseconds = tv.tv_usec/1000;
+    *milliseconds = tv.tv_nsec/1000000;
 }
@@ -291,1 +297,1 @@
-    time_t now = time(NULL);
+    time_t now = monotonicSeconds();
